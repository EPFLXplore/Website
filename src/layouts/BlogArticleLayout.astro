---
const { title, category, image, author, date } = Astro.props;
---

<article>
  <h1>{title}</h1>
  <p><strong>Category:</strong> {category}</p>
  <img src={image} alt={title} style="max-width:100%;height:auto;" />
  <p><strong>Author:</strong> {author}</p>
  <p><strong>Date:</strong> {date}</p>
  <slot />
</article>
